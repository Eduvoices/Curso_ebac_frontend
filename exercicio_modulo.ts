function multiplicação(fator1: number, fator2: number): number{
    return fator1 * fator2
}

console.log(multiplicação(5,8))

function saudação(nome: string): string {
    return `Olá ${nome}`
}

console.log(saudação('Gandalf'))